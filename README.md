# 브랜드 이미지 레퍼런스 라이브러리

회사 공식 웹사이트가 공개한 이미지 중 디자이너가 참고하기 좋은 자산을 회사와 업종별로 정리한 정적 웹 라이브러리입니다.

## 현재 수집 현황

- 회사: 291개
- 고유 이미지 파일: 4,845개
- 회사별 이미지 연결: 4,928건
- 파비콘: 265개
- 소셜 대표 이미지: 224개
- 로고 분류: 874개
- 배너 분류: 1,083개
- 기타 참고 이미지 분류: 2,433개
- 전체 이미지 용량: 약 394MiB
- 심층 확인 회사: 86개
- 내부 페이지: 1,066개
- PC 응답 페이지: 1,032개
- 모바일 응답 페이지: 1,047개

특히 국내 렌터카 36곳, 글로벌 렌터카 34곳과 장기렌트·리스 우선 회사 21곳을 확장 확인했습니다. 완전히 같은 파일은 저장소 전체에서 한 번만 저장하고 각 회사의 분류와 출처 정보만 공유합니다.

## 웹에서 할 수 있는 일

- 회사명과 페이지 키워드 검색
- 업종별 필터
- 배너, 로고, 소셜, 기타 참고, 파비콘 유형별 필터
- 배너와 로고 수집 여부별 필터
- 장기렌트·리스 등 업종별 분류
- 이미지 수, 회사명, 업종 기준 정렬
- 회사별 전체 이미지 모달 보기
- PC, 모바일, 공통 구분과 해상도, 파일 형식, 용량 확인
- 이미지 파일 저장과 원본 출처 확인
- 이미지별 하트와 찜한 이미지만 모아보기
- 동기화 코드로 다른 컴퓨터에서도 같은 찜 목록 사용
- 라이트 모드와 다크 모드 전환

## 파일 구조

```text
assets/업종/회사-slug/
  favicon.*
  og.*
  logo-001.*
  banner-001.webp
  reference-001.*
  source.json

index.html
app.css
app.js
manifest.json
manifest.csv
reference_collector.py
deep_reference_collector.py
priority_companies.json
```

`manifest.json`과 회사별 `source.json`에는 이미지 유형, PC·모바일 구분, 발견 페이지, 저장 경로, 원본 URL, 해상도, 용량, 파일 형식과 해시가 기록됩니다.

## 로컬에서 보기

파일 요청을 사용하는 웹 페이지이므로 폴더에서 간단한 웹 서버를 실행한 뒤 접속합니다.

```powershell
python -m http.server 4173
```

브라우저에서 `http://localhost:4173/`을 엽니다.

## 다시 수집하기

전체 목록의 첫 화면을 다시 확인하려면 다음 명령을 사용합니다.

```powershell
python reference_collector.py --workers 12
```

장기렌트·리스 우선 회사를 내부 페이지와 PC·모바일 화면까지 다시 확인하려면 다음 명령을 사용합니다.

```powershell
python deep_reference_collector.py --priority --workers 4
```

특정 회사나 업종만 심층 확인할 수도 있습니다.

```powershell
python deep_reference_collector.py --company 롯데렌터카
python deep_reference_collector.py --category 렌터카_국내 --workers 4
```

## 수집 기준

- 로고: 구조화 데이터, 헤더, 내비게이션, 파일명과 대체 텍스트를 기준으로 선별
- 배너: 메인 비주얼, 히어로, 프로모션, 캠페인 영역과 이미지 비율을 기준으로 선별
- 기타 참고: 공식 사이트의 제품, 서비스, 콘텐츠 이미지 중 충분한 해상도를 가진 항목 선별
- 내부 링크, 사이트맵, HTML, CSS 배경, 스크립트 이미지 주소를 함께 확인
- 각 논리 페이지를 PC와 모바일 브라우저 식별자로 각각 요청해 별도 자산을 구분
- 배너와 참고 이미지는 최대 2200px WebP로 최적화
- SVG 로고와 투명 배경 로고는 가능한 원형을 유지
- 원본 해시와 지각 해시로 완전히 같거나 사실상 같은 파일을 제거

공식 사이트가 자동 접근을 차단하거나 robots 정책상 수집이 허용되지 않거나 실행 후에만 이미지를 제공하는 경우 새 이미지가 비어 있을 수 있습니다. 이런 회사도 기존 파비콘과 소셜 대표 이미지는 유지하며, 웹 화면에서 미수집 상태를 확인할 수 있습니다.

## 찜 목록 동기화

하트는 먼저 현재 브라우저에 저장됩니다. `기기 간 동기화 설정`에서 코드를 만든 뒤 다른 컴퓨터에 같은 코드를 입력하면 동일한 찜 목록을 불러옵니다. 코드는 비밀번호 역할도 하므로 공개하지 않아야 하며, 무료 온라인 보관함은 30일 동안 사용하지 않으면 만료될 수 있습니다.

## 출처와 사용 범위

모든 이미지의 권리는 각 회사와 원저작권자에게 있습니다. 이 저장소는 디자인 조사와 레퍼런스 확인을 위한 자료 모음이며, 상업적 재사용 전에는 각 출처의 이용 조건과 브랜드 가이드를 확인해야 합니다.
